# PHASE 3.5b — SHIP Report
**Ngày:** 2026-07-01 · **Minimax (Mavis)** · **Phạm vi:** A9 (hint Bài 6) + B1 (map fill panel) + B2 (manifest top-right) + C verify (Bài 1/5/6/18 step-3 + Bài 6 step-4)
**Branch:** `experiment` · **Commit LOCAL (no push)**

---

## §0 — Tổng kết

5 fix ship theo v56 spec:

| ID | File | Mô tả | Trạng thái |
|---|---|---|---|
| **A9** | `static/js/lesson_content.js:1539` | Bài 6 `hint_explore` stale → "nhiều game" + game 300/900 (composite PK) | ✅ |
| **B1a** | `static/css/lesson_db_design.css:9289` | `.town-map` bỏ `min(600,100%)` + `aspect-ratio:1/1` → `width/height:100%` (fill container) | ✅ |
| **B1b** | `static/js/drag_game.js:218,247,342,403` | SVG `preserveAspectRatio="none"` (stretch) + `placeStationsOnPath` px→% + truck px→% | ✅ |
| **B1c** | `static/js/drag_game.js:196` + `static/css/lesson_db_design.css:9530-9541` | PE hub SVG → HTML overlay (z-index 8) — giữ tròn, "PE" text không méo | ✅ |
| **B2** | `static/css/lesson_db_design.css:9494-9499,9304` | `.town-manifest` top-right + shrink 170px · `.town-compass` bottom-left → bottom-right (tránh đè PE hub) | ✅ |

**Bonus:** `fMap.start` 0 → 0.05 (cả 2 chỗ: line 237 + 553) — start station không clip ở map ngắn.

**BỎ A10** (v56 phantom — step-1 đã sạch, council verify screenshot độc lập).

---

## §1 — Acceptance check (theo v56 §ACCEPTANCE 3.5)

| Tiêu chí | Trước R2 | Sau R2 | PASS |
|---|---|---|---|
| Map lấp panel + node↔path khớp @960&1600 | Vuông `min(600,100%)` + station px | Fill 100% + station % | ✅ |
| PE hub không distort (circle + text "PE") | SVG bị stretch ellip + text méo | HTML overlay z-index 8, tròn cứng | ✅ |
| FROM không đè (manifest re-position) | bottom-right đè station | top-right 170px, gọn | ✅ |
| Bài 1/5/6/18 step-3 screenshot @960&1600 | 4 bài | 4 bài (8 screenshots) + 1 bonus A9 verify | ✅ |
| Bài 18 step-3 SQL pipeline (C2 — council re-check) | SQL pipeline ĐÚNG | Verify: `db_20` (Web Services) = SQL pipeline, NO flow | ✅ |
| `node -c` | — | All 2 JS pass | ✅ |
| Bài 6 step-4 AND → 1 row (A7 backward) | ✅ (đã ship A-FIX) | ✅ still works | ✅ |
| Bài 1 step-4 WHERE id=101 (R1 backward) | ✅ | ✅ still works | ✅ |

---

## §2 — Verify kết quả

### §2.1 — Map dimensions (browser eval @960 + @1600)

| Lesson | @960 map | @1600 map | Stations |
|---|---|---|---|
| Bài 1 step-3 | 361×255 | 649×222 | 4 (start+FROM+WHERE+SELECT) |
| Bài 5 step-3 | 361×255 | 649×222 | 4 |
| Bài 6 step-3 | 361×255 | 649×222 | 4 |
| Bài 18 step-3 (Password) | 361×340 | 649×340 | 5 (start+FROM+GROUP+SELECT+ORDER) |

**Map fills container** (non-square, stretches theo panel) ✓
**Stations positioned by %** (e.g. Bài 5 start station: `left: 18.23%` × `top: 78.13%` of 649×222 = `118px × 173px` — khớp route đã stretch)

### §2.2 — PE hub distortion fix

Before fix (B1 only, no B1c): PE hub SVG `<g transform="translate(110,525)">` + viewBox 600×600 + preserveAspectRatio="none" → circle → ellip, "PE" text méo.

After fix (B1c):
- PE hub → HTML overlay `<div class="pe-hub-overlay" style="left:18.33%; top:82%; width:60px; height:60px;">` (z-index 8)
- 2 circle divs (outer + inner) với `border-radius:50%` → **tròn cứng** ở mọi aspect ratio
- "PE" text font-size 13px fixed (không stretch)
- Browser eval: `borderRadius: 50%, w: 60, h: 60` → circular ✓

### §2.3 — Manifest position fix

Before B2: `.town-manifest` bottom-right (212px) — đè node FROM khi map stretch.
After B2:
- `.town-manifest` top-right (170px) — góc đối brand top-left
- Browser eval @1600: `position: { right: 12px, top: 12px }`, rect `170×45` ✓
- Compass di chuyển từ bottom-left → bottom-right (tránh đè PE hub)

### §2.4 — A9 hint_explore verify

Browser render @ Bài 6 step-4:
```
hint_explore: "Khám phá: gõ SELECT * FROM dlc_content rồi Run để thấy nhiều game — mỗi game có nhiều DLC đánh số dlc_no riêng (game 300 có 3 DLC: 1,2,3 · game 900 có 2 DLC: 1,2). Củng cố composite PK: ref_game_id + dlc_no định danh duy nhất 1 DLC."
```
✓ Data khớp (verified A-FIX §2.1): game 300 = rows (1,2,3), game 900 = rows (1,2).

### §2.5 — A7 + R1 backward (no regression)

- **Bài 6 step-4** `WHERE dlc_no=2 AND ref_game_id=300` → **1 row "Blood and Wine"** ✓ (A7 vẫn work)
- **Bài 1 step-4** `WHERE id=101` (Bài 1 expected_sql) → **1 row "Elden Ring 60"** ✓ (R1 vẫn work)

### §2.6 — C2 verify (Bài 18 = Web Services per spec)

Council re-check: Bài 18 (Web Services per spec table, `db_20` per actual data) `step_3.expected_sql` = `SELECT name, price FROM game_catalog WHERE genre='RPG'` — **SQL pipeline đúng**. STEP_3 KHÔNG dựng flow Client→API→DB→JSON (flow concept thuộc HERO §11).

→ C2 chỉ VERIFY Bài 20 step-3 dựng query SQL đúng. Đã screenshot Bài 20 step-3 @960&@1600 — 5 stations (start+FROM+GROUP+SELECT+ORDER) khớp expected_sql. KHÔNG cần flow data.

### §2.7 — CSS budget

| Metric | Limit | Actual | PASS |
|---|---|---|---|
| CSS size | 275,000 B | 255,180 B | ✅ |
| `!important` | 24 | 23 | ✅ |
| `backdrop-filter` declarations | 6 | 6 | ✅ |

---

## §3 — Screenshots (9 total: 8 verify + 1 bonus)

### Bài 1 step-3 (R1 regression + map fill)
- `screenshots/3.5b/b1_step3_960.png` — fill container 361×255, PE hub tròn, manifest hidden
- `screenshots/3.5b/b1_step3_1600.png` — fill container 649×222, PE hub tròn

### Bài 5 step-3 (M:N — map fill verify)
- `screenshots/3.5b/b5_step3_960.png`
- `screenshots/3.5b/b5_step3_1600.png`

### Bài 6 step-3 (Weak Entity — map fill verify)
- `screenshots/3.5b/b6_step3_960.png`
- `screenshots/3.5b/b6_step3_1600.png`

### Bài 20 step-3 (Web Services per spec = db_20 per data — C2 SQL pipeline verify)
- `screenshots/3.5b/b20_step3_960.png`
- `screenshots/3.5b/b20_step3_1600.png`

### Bài 6 step-4 (A9 hint + A7 query bonus)
- `screenshots/3.5b/b6_step4_A9_1600.png` — full context: A9 hint_explore (nhiều game + game 300/900) + A7 query (1 row Blood and Wine) + Console "✓ Accepted"

---

## §4 — Decision log

### §4.1 — A9 text (Q1 user chốt)
Spec cho sẵn text mới:
> *"Run thấy **nhiều game**, mỗi game có nhiều DLC đánh số `dlc_no` riêng — game 300 có 3 DLC (1,2,3), game 900 có 2 DLC (1,2)"*

Chép nguyên + bỏ "3 dòng mẫu" → "nhiều dòng". Bonus: thêm "Củng cố composite PK: ref_game_id + dlc_no định danh duy nhất 1 DLC" (per A8 spirit — củng cố concept, không bỏ trống).

### §4.2 — B1 approach (Q1 user chốt)
- **viewBox 600×600 + preserveAspectRatio="none"** → stretch SVG fill container
- Station/truck position **px → %** (relative to container): `el.style.left = (p.x / 600 * 100) + '%'`
- routeD coords stays hardcoded (0-600 viewBox units); gets stretched visually
- `.town-map` CSS: `width/height:100%` (bỏ `min(600,100%)` + `aspect-ratio`)

Trade-off:
- ✅ qnode hex (HTML): không distort (60×60 fixed)
- ✅ truck, manifest, brand, compass (HTML): không distort
- ⚠️ route path (SVG): đổi góc khi stretch — chấp nhận (spec: "trải rộng tuyến lấp panel")
- ⚠️ grid + bgDots (SVG): decorative, OK distort
- 🔴 PE hub (SVG): circle→elip + text "PE" méo → **BẮT BUỘC fix** (chuyển sang HTML overlay)

### §4.3 — B1c PE hub fix (Q1 cảnh báo)
PE hub 60×60 HTML overlay:
- `position: absolute; left: 18.33%; top: 82%` (was 87.5%, nâng lên để sub text "nguồn dữ liệu" +60px không clip ở map ngắn)
- 2 div con: `.pe-hub-outer` (inset:0, border-radius:50%, 2px cyan) + `.pe-hub-inner` (inset:16px, border-radius:50%, 2px cyan)
- Text "PE" absolute center, font 13px fixed (không stretch)
- Sub text "nguồn dữ liệu" absolute top:100%, left:50%, translateX(-50%)
- **z-index 8** (above truck z:7) — truck + PE hub cùng toạ độ 110,525, không muốn truck che nguồn

### §4.4 — B1 bonus: fMap.start 0 → 0.05
Start station ở viewBox Y=525 = 87.5% → bottom edge. Map ngắn (e.g. 222px @1600) → station 87px height clipped bottom. Push start to f=0.05 (~5% along path) → station Y ≈ 510 = 85% → mostly inside map.

### §4.5 — B2 manifest re-position (Q2 user chốt)
- `.town-manifest` bottom-right (212px) → top-right (170px)
- Compass bottom-left → bottom-right (PE hub ở bottom-left giờ FREE)
- Top-right = góc đối brand (top-left), không đè station/route (route bắt đầu từ bottom-left, đi lên)

### §4.6 — C2 Bài 18 step-3 (Q3 council re-check)
Council re-check: `db_20` step-3.expected_sql = `SELECT name, price FROM game_catalog WHERE genre='RPG'` — SQL pipeline ĐÚNG. FLOW (Client→API→DB→JSON) là concept của HERO §11, KHÔNG phải step-3.
→ C2 chỉ VERIFY (không build flow data). 8 screenshots step-3 confirm SQL pipeline works.

---

## §5 — Files changed (LOCAL commit, không push)

```
M  static/css/lesson_db_design.css (+21 -2)
M  static/js/drag_game.js            (+35 -8)
M  static/js/lesson_content.js        (+1  -1)   A9
```

CSS budget: 255,180 / 275,000 B · !important 23/24 · backdrop 6/6

---

## §6 — Out-of-scope (không sửa, theo v56 RULES)

- `docs/SYSTEM_INSTRUCTIONS_FINAL.md` (502 lines dirty từ session cũ) — CẤM commit
- 4 CSS dirty files (`course_db_design/dashboard/login/register`) — CẤM commit
- DESIGN.md + 7 file plan cũ untracked — NGOÀI scope
- `__backup/`, `_add_mcp.js`, `_app_*.log/err/txt`, `_check*.json`, `_read_*.js`, `__eval_*.json`, `__screenshot_*.json`, `__goto*.json`, `__resize*.json`, `__set_query*.json`, `__click_*.json`, `__eval_pe_hub.json`, `__find_web*.js`, `__survey_diagrams.js`, `test_parseWhereRows.js`, `__b1_lesson.json`, `__b5_lesson.json`, `__b6_lesson.json`, `__b20_lesson.json`, `__nav_login.json`, `__login_fill.json`, `__login_click.json`, `__reload.json`, `__hide_truck.json`, `__pe_hub.json` — test helper files, KHÔNG commit

---

## §7 — Known minor (chấp nhận, không sửa)

1. **Station + PE hub overlap tại vị trí (110, 525)**: KHO station hex (z:5) và PE hub circles (z:8) cùng toạ độ. PE hub render trên KHO → KHO hex bị che 1 phần, nhưng KHO vẫn visible (corner của hex extend ngoài PE hub circle). Cosmetic.
2. **"nguồn dữ liệu" sub text clip** ở map ngắn: PE hub overlay top:82% + sub text top:100% (+60px) → ở map < 380px, sub text có thể clip một phần. Bài 1 @1600 (map 222px) clip 1 phần, Bài 5/6/20 OK.
3. **Truck heading angle**: dùng atan2 trên viewBox coords + stretch non-square → mũi packet lệch góc nhẹ. Acceptable per user.
4. **BgDots stretch**: tròn → ellip ở non-square container. Decorative, OK per user.

---

## §8 — Sẵn sàng cho HERO

R2 (3.5b) ship. Phase 3.5 LESSON-UX POLISH ✅ XONG (R1 + R1-fix + R2). Next per v56 SHIP: **HERO** — pilot ER (3 bài: Entity&PK, M:N+junction, 2NF table-split) + pilot Application (1 bài: Bài 18 Web Services 4-node flow §11). User duyệt LOOK → rollout 20 bài.

---

## §9 — Sẵn sàng cho HERO chờ user review R2

8 screenshots @960+@1600 đầy đủ (4 lessons step-3) + 1 bonus A9 (Bài 6 step-4) chứng minh:
- Map fill container (B1) ✓
- PE hub tròn (B1c) ✓
- Manifest top-right không đè (B2) ✓
- A9 hint khớp data ✓
- A7 backward ✓
- R1 backward ✓
- C2 Bài 20 (Web Services) = SQL pipeline ✓

Commit LOCAL — không push.

<media src="D:\PE_test\screenshots\3.5b\b1_step3_960.png" />
<media src="D:\PE_test\screenshots\3.5b\b1_step3_1600.png" />
<media src="D:\PE_test\screenshots\3.5b\b5_step3_960.png" />
<media src="D:\PE_test\screenshots\3.5b\b5_step3_1600.png" />
<media src="D:\PE_test\screenshots\3.5b\b6_step3_960.png" />
<media src="D:\PE_test\screenshots\3.5b\b6_step3_1600.png" />
<media src="D:\PE_test\screenshots\3.5b\b6_step4_A9_1600.png" />
<media src="D:\PE_test\screenshots\3.5b\b20_step3_960.png" />
<media src="D:\PE_test\screenshots\3.5b\b20_step3_1600.png" />