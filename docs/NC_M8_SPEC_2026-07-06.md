# NC MODULE 8 — Giao dịch & Concurrency (Trading Floor) · Spec đợt 7

> 2026-07-06 · User chốt 4/4 recommended qua AskUserQuestion. Nguồn: PART_7.pdf (roadmap 14 bài Ch.18-19)
> + Silberschatz 7ed Ch.18 intro + 18.1 (trích scratchpad book/ch18_intro_181.txt).
> Màu module 8: Rose #FB7185 (đã khai MODULE_COLORS từ đợt shell). module_title: 'Trading Floor — Giao dịch & Concurrency'.

## Quyết định đợt 7 (user chốt)
1. **Phạm vi**: nc_11 (bài 1 — "Vì sao chạy đồng thời gây lỗi?", Ch.18 intro) + nc_12 (bài 2 — "S/X Locks", 18.1.1-18.1.2). Ticket #52-53. CHƯA có card (Card A Strict/Rigorous đến sau bài 3 — đợt sau).
2. **Kịch bản NEO module**: VÍ GEM seller (banking A/B của sách map sang ví DragonForge). Flash-sale "2 khách 1 món" để dành cho deadlock/phantom.
3. **Sim thứ 5 `txn_visual`** (nc_11 + nc_12 dùng chung renderer, data-driven schedule): stepper 2 chế độ kiểu flow sim — 2 cột T1/T2 + VÍ GEM giữa, bấm từng nhịp; so kèo cuối.
4. **Step-4**: nc_11 mcq_code (chọn kết cục ví sau schedule xen kẽ) · nc_12 fill_blank (S/X/550). Anti-boredom OK: nc_10 bug_fix → nc_11 mcq → nc_12 fill_blank.

## Ràng buộc engine (probe_txn.js 2026-07-06)
- `BEGIN/COMMIT`, `LOCK TABLE`, `SET TRANSACTION` → lỗi cú pháp TO ("Cú pháp: SELECT … FROM …") — KHÔNG silent-wrong, nhưng không dạy gõ được → step-4 M8 không dùng transaction syntax.
- `SELECT … FOR UPDATE` → clause bị NUỐT im lặng nhưng rows trả về ĐÚNG (WHERE vẫn áp) — dùng được cho bài 9 (FOR UPDATE), khi tới phải soi validateSQL xử FOR UPDATE thế nào.

## Số canonical M8 (nối world M7)
- **Ví DragonForge (seller 4102): 500 gem** lúc 23:59:59.
- **T1** = khách MythicSlayer88 (buyer 88) trả **+100 gem** (mua skin Hỏa Long 100 gem).
- **T2** = DragonForge **rút −50 gem**.
- Serial (bất kỳ chiều nào): **550 ✓**. Lost update (T1 đọc · T2 đọc · T1 ghi 600 · T2 ghi 450): **450 ❌ — +100 của khách bốc hơi**. Xen chiều kia (T2 ghi trước, T1 ghi 600 đè): mất 50 của lệnh rút.
- Ma trận S/X (Fig 18.1): chỉ **S+S ✓**; S+X, X+S, X+X → CHỜ.
- Có khóa X: T2 xin X phải XẾP HÀNG → T1 ghi 600 → unlock → T2 grant, đọc 600 → ghi **550 ✓**.
- Trap sách (nuôi bài 3 — 2PL): unlock QUÁ SỚM vẫn sai — banking A=100/B=200, T2 đọc tổng ra **$250 thay vì $300** (T1 unlock(B) rồi mới lock(A)).

## Khối bịa step-3
- nc_11: "Database thấy 2 người cùng sửa một ví thì tự báo lỗi, chặn người đến sau" — bịa: không khóa/không cơ chế thì DB im re, ghi sau đè im lặng.
- nc_12: "Đọc/ghi xong thì unlock NGAY cho người khác đỡ chờ" — trap unlock-sớm ($250), reveal chốt treo 2PL.

## Ghi chú tồn đọng module
- `graduation: 25` là ĐÚNG (đính chính nghi vấn "24 bài"): roadmap course_db_design.js khai 25 bài — 14 bài PART_7 (nc_11..24) + **bài 25 = Boss Battle "Engine Under Fire"** (query chậm + 100 user + crash). Trophy 20 = khép M8 v2.0 ✓; boss phải thiết kế khi khép M9.
- Card "Lock vs MVCC" (ngoài PART_7, đề xuất từ audit cũ) — chốt khi tới bài 8 MVCC (nc_18).
- PART_7 card map M8: A Strict/Rigorous (sau bài 3) · B Wait-die/Wound-wait (sau bài 4) · C Lock Escalation (sau bài 5) · D Predicate Locking (sau bài 6) · E Thomas' Write Rule (sau bài 7) · F Degree-Two/Cursor Stability (sau bài 10).

---
## ADDENDUM ĐỢT 8 (2026-07-06) — nc_13 2PL + nc_14 Deadlock + Card A/B
User chốt 4/4 recommended: (1) nc_13+nc_14+Card A/B, chuỗi CTA b13→A→b14→B→b15; (2) sim thứ 6 `phase_visual` — phase-meter 2PL bấm từng lệnh, thanh "số khóa giữ" dâng/hạ, LOCK POINT badge, bản NHẢ SỚM bật đỏ VI PHẠM + tái hiện $250; (3) sim thứ 7 `wfg_visual` — wait-for graph builder: thêm cạnh từng yêu cầu từ lock table, đủ vòng tô đỏ DEADLOCK, bấm node chọn victim (chọn ngoài vòng/victim đắt có feedback riêng); (4) step-4: nc_13 mcq_code (chọn bản 2PL hợp lệ giữa 4 kịch bản lock/unlock) + nc_14 fill_blank (đọc graph: 3 / T4 / A).

Số & kịch bản canonical đợt 8 (bám sách, đổi $ thành gem):
- nc_13: DragonForge chuyển 50 gem két CHÍNH (B, 200) → két QUẢNG CÁO (A, 100); tổng thật 300. T-audit đọc tổng. Nhả B sớm rồi mới gom A (T1 sách) → audit chen đọc 250 SAI. Bản 2PL (T3 sách): gom X(B), X(A) [LOCK POINT] → làm việc → nhả cả hai → audit chen lúc nào cũng 300. 2PL vẫn KHÔNG thoát deadlock (Fig 18.7: T3/T4 kẹt nhau) → cliffhanger bài 14.
- nc_14: map Fig 18.13-14 sang GameHub, node T1..T4 (=T17..T20 sách): cạnh lần lượt T1→T2, T1→T3, T3→T2, T2→T4 (CHƯA vòng — "chờ dài chưa chết"), cạnh 5 T4→T3 → vòng T2→T4→T3→T2. T1 chờ vào vòng nhưng KHÔNG nằm trong vòng (không phải victim hợp lệ). Victim chuẩn T4 (trẻ nhất, ít khóa, chưa ghi — 4 tiêu chí cost sách 18.2.2.2 + đếm số lần bị tế để chống starvation); chọn T2/T3 = phá được nhưng đắt.
- Card A Strict vs Rigorous (sau b13): strict giữ X đến commit → chặn cascading rollback (Fig 18.8 T5/T6/T7); rigorous giữ MỌI khóa → serial theo thứ tự commit; thương mại dùng rộng rãi. CTA → ?lesson=14.
- Card B Wait-die vs Wound-wait (sau b14): timestamp già/trẻ, T14(5)/T15(10)/T16(15) sách; wait-die già chờ trẻ chết; wound-wait già đâm trẻ chờ; rollback giữ nguyên timestamp cũ chống starvation; + timeout scheme hạn chế. CTA tạm → /courses/db_design_nc (bài 15 chưa có — ĐỢT SAU PHẢI REPOINT về ?lesson=15).

---
## ADDENDUM ĐỢT 9 (2026-07-07) — nc_15 Granularity + nc_16 Phantom + Card C/D
User chốt 4/4 recommended: (1) nc_15+nc_16+Card C/D, chuỗi b15→C→b16→D→b17, repoint Card B → ?lesson=15; (2) sim thứ 8 `lock_tree_visual` — cây SÀN → bảng → dòng, bấm từng nhịp: T1 cắm IX→IX→X xuống dòng #3001; T2 xin S(bảng listings) đụng biển IX ngay NODE BẢNG → chờ (khỏi soi 40.000 lá); T3 IS→IS→S(dòng #3002) sống chung; (3) nc_16 TÁI DÙNG txn_visual — "ví" thành bộ đếm COUNT(price<100)=3: mode TUPLE-LOCK → T2 chèn Kiếm gỗ 45 lọt (ma), T1 đếm lại 4 ❌; mode INDEX-LOCKING → T2 xin X(lá [0..100)) bị S của T1 chặn → đếm lại vẫn 3 ✓; (4) step-4: nc_15 mcq_code (bộ khóa IX sàn→IX bảng→X dòng) + nc_16 full_ide `SELECT COUNT(*) FROM listings WHERE price < 100;` (probe c1-c4: COUNT(*)± WHERE đúng 6→3).

Số & luật canonical đợt 9 (Ch.18.3-18.4):
- Ma trận Fig 18.16: IS↔{IS,IX,S,SIX} · IX↔{IS,IX} · S↔{IS,S} · SIX↔{IS} · X↔∅. IX+IX SỐNG CHUNG (xung đột thật nằm dưới lá).
- 5 luật cây: khóa ROOT trước (mode nào cũng được) · S/IS cần cha IS|IX · X/SIX/IX cần cha IX|SIX · phải 2-phase · nhả bottom-up (con sạch mới nhả cha).
- Lock escalation (Card C): lock table quá tải → đổi vạn tuple-lock lấy 1 relation-lock; relation scan → khóa bảng, index scan ít dòng → intention + tuple.
- Phantom (map count(*) Physics + insert Feynman): T1 đếm <100 = 3; T2 chèn 45 gem — tuple MỚI chưa tồn tại nên tuple-lock mù; đếm lại 4. Update vào vùng predicate cũng là ma. Thuốc: index-locking — lookup S-lock lá; insert/delete/update X-lock lá bị ảnh hưởng → xung đột ma thành xung đột thật. Predicate locking (Card D) chính xác nhất nhưng đắt, ít dùng.
- Khối bịa: nc_15 "xin thẳng X tại dòng, khỏi phiền tầng trên" (phá luật cha-trước-con); nc_16 "đếm 2 lần khớp nhau là chắc không ma" (chính vụ 3→4 bác bỏ).
- Card D CTA tạm → /courses/db_design_nc — ĐỢT SAU REPOINT về ?lesson=17.

---
## ADDENDUM ĐỢT 10 (2026-07-07) — nc_17 Optimistic + nc_18 MVCC/Snapshot + Card E + Card Lock-vs-MVCC
User chốt 4/4 recommended: (1) nc_17+nc_18+Card E+Card Lock vs MVCC (trả món nợ backlog "chốt khi tới nc_18"), chuỗi b17→E→b18→LvM→b19, repoint Card D → ?lesson=17; (2) sim thứ 9 `valid_visual` — "bàn nháp 3 pha": 2 cột T1/T2 + workspace nháp riêng, thanh READ→VALIDATE→WRITE sáng theo nhịp, mode PASS (T2 read-only) / mode ABORT (T2 commit ghi đè món T1 đã đọc → xé nháp làm lại, DB không bẩn vì nháp chưa từng chạm DB); (3) sim thứ 10 `mvcc_visual` — version timeline: chuỗi version 1 món + tem CommitTS, mode READER (StartTS chọn version tem lớn nhất ≤ StartTS, không chờ không abort) / mode FIRST-COMMITTER-WINS (2 updater cùng món, đứa validate sau thấy version lạ trong khoảng (StartTS, CommitTS] → abort — chặn lost update); (4) step-4: nc_17 fill_blank (3 ca phán quyết validation PASS/ABORT/PASS) + nc_18 mcq_code (reader StartTS=30 thấy giá nào — bẫy "mới nhất"/"bản đầu"/"chờ writer"). Anti-boredom: nc_16 full_ide → nc_17 fill_blank → nc_18 mcq_code ✓.

Số & luật canonical đợt 10 (Ch.18.5.3 + 18.6 + 18.7-18.8; write skew + FOR UPDATE ĐỂ DÀNH bài 19 theo roadmap):
- nc_17 (18.6): Khiên Hắc Long #7042 giá 400. T1 đọc 400 → nháp 450 (chỉnh giá +50). Mode PASS: T2 chỉ xem → validate ✓ chép 450. Mode ABORT: T2 flash-sale commit 400→380 giữa chừng → writeset(T2)∩readset(T1)={#7042} → T1 abort, restart từ 380. 3 tem: StartTS / ValidationTS / FinishTS. Test sách (Tk có TS < Ti): FinishTS(Tk) < StartTS(Ti) HOẶC (writeset(Tk)∩readset(Ti)=∅ và FinishTS(Tk) < ValidationTS(Ti)). Không cascading rollback vì write chỉ chạm DB SAU validation. Trả giá: long transaction đói (starvation) nếu bị đám ngắn tông hoài.
- Step-4 nc_17 fill_blank — T-audit Start=20, Validation=35: ca1 Tk Finish=12 (<20, xong trước khi mình start) → PASS; ca2 Tk commit=28 GHI món mình đã đọc → ABORT; ca3 Tk commit=30 ghi món mình KHÔNG đọc → PASS.
- nc_18 (18.7-18.8): Kiếm Rồng #3001 (nối world nc_15) chuỗi version v1=500 (tem 10) → v2=480 (tem 25) → v3=520 (tem 40). Reader StartTS=30 → 480 (tem lớn nhất ≤30); StartTS=15 → 500; StartTS=45 → 520. Writer không chặn reader — ghi = DÁN version mới, không tẩy bản cũ. First committer wins: T5 Start=50 / T6 Start=52 cùng đọc 520 cùng sửa; T6 validate trước CommitTS=60 dán v4=470; T5 validate CommitTS=65 thấy tem 60 ∈ (50,65] trên món định ghi → ABORT. Dọn version: bản cũ hơn 2 bản đều già hơn transaction già nhất → xóa được.
- Card E Thomas' Write Rule (sau b17): schedule 4 sách (T27 read Q · T28 write Q · T27 write Q muộn) — timestamp protocol bắt rollback T27; Thomas: bản ghi cũ rích không ai sẽ đọc → LỜ ĐI khỏi rollback; mở view serializability (Note 18.1); nền timestamp 18.5 dạy mức mental model "già/trẻ — đọc/ghi quá muộn thì rollback, không ai chờ nên không deadlock". CTA → ?lesson=18.
- Card Lock vs MVCC (sau b18, NGOÀI PART_7 — trả nợ audit): pessimistic (chặn trước: chờ + deadlock + reader kẹt writer) vs optimistic/MVCC (chạy trước soát sau: reader không bao giờ chờ, trả giá abort/restart + dọn version); Oracle/PostgreSQL/SQL Server đều chơi snapshot; chọn theo workload va-chạm-cao ↔ read-heavy. CTA tạm → /courses/db_design_nc — ĐỢT SAU REPOINT về ?lesson=19.
- Khối bịa: nc_17 "nháp ghi thẳng vào DB cho nhanh, rớt validation DB tự lau giùm" (sai — write sau validation mới chạm DB, nhờ thế không cascading rollback); nc_18 "writer ghi đè chỗ cũ, reader chờ writer xong cho chuẩn" (sai — MVCC dán version mới, reader đọc bản cũ, không ai chờ ai).

---
## ADDENDUM ĐỢT 11 (2026-07-07) — nc_19 Write Skew + nc_20 Version Numbers + Card F — KHÉP M8
User chốt 4/4 recommended: (1) nc_19+nc_20+Card F khép module, trophy 2 "Bậc thầy MVCC" + ship MARKETPLACE v2.0 nổ tại bài 20, trả nợ repoint Card Lock-vs-MVCC → ?lesson=19; (2) nc_19 TÁI DÙNG txn_visual lần 3 — "ví" thành đồng hồ TỔNG 2 KÉT = 300; (3) nc_20 sim thứ 11 `seat_visual` — lưới ghế GameHub Arena (kịch bản PART_7 chỉ định), mode VERSION-CHECK vs mode KHÓA-SUỐT-5-PHÚT; (4) step-4: nc_19 bug_fix (SELECT 2 két THIẾU FOR UPDATE — vá chính cái bug gây write skew) + nc_20 fill_blank (8 / 0 / ABORT). Rotation: nc_18 mcq_code → nc_19 bug_fix → nc_20 fill_blank ✓.

Gia cố grader (validateSQL/extractClauses): regex WHERE trước đây NUỐT "FOR UPDATE" vào đuôi WHERE → user thiếu FOR UPDATE nhận message "WHERE không khớp" vừa lộ nguyên đáp án vừa gọi sai tên. Sửa: FOR UPDATE thành stop-token của WHERE/ORDER BY + clause riêng `forUpdate` + message riêng hai chiều (thiếu → "đọc mà không giữ chỗ"; thừa → "chặn oan"). Exact-match path không đổi; các bài cũ không có FOR UPDATE trong expected → không regression.

Số & luật canonical đợt 11 (Ch.18.8.3 + 18.9):
- nc_19 (Ticket #60): két A QUẢNG CÁO = 100 · két B CHÍNH = 200 (nối world nc_13), luật sàn "TỔNG 2 két ≥ 0" (cho phép 1 két âm tạm — phí quảng cáo trừ sau). T1 rút 200 từ A, T2 rút 200 từ B — mỗi đứa soát luật trên snapshot RIÊNG: 300−200 = 100 ✓ → cùng commit → A=−100, B=0, TỔNG = −100 ❌ (map T36/T37 sách). Cửa soát FCW mù vì writeset {A} ∩ {B} = ∅ — write skew = đọc chéo, ghi khác món. Fig 18.20 (A=B/B=A swap) làm mcq; hóa đơn trùng số max+1 (chuyện thật IIT Bombay, footnote sách) làm classify. Thuốc: FOR UPDATE — món ĐỌC bị coi như ĐÃ GHI → hai giao dịch "cùng ghi" A lẫn B → chỉ 1 đứa qua; T2 abort, retry thấy tổng 100 → lệnh rút bị luật sàn từ chối → tổng chốt 100 ✓. SSI (Postgres 9.1+): theo dõi rw-conflict, đứa có cả cạnh vào lẫn ra bị tế — nhắc mức card/mcq.
- nc_20 (Ticket #61): ghế C4 version = 7. Đọc = GIAO DỊCH RIÊNG (nhả khóa ngay, không khóa nào sống qua thời-gian-suy-nghĩ); user nghĩ 4 phút, MythicSlayer88 chốt C4 trước → version 7→8; user chốt: UPDATE … WHERE seat='C4' AND version=7 → trúng 0 dòng → ABORT, app mời chọn ghế khác (C5, version 3). Khớp version → update + version +1, tất cả trong 1 giao dịch DB atomic. Sách: = first-committer-wins thủ công, "optimistic concurrency control without read validation" — Hibernate gọi conversations. 2PL giữ S cả khoang trong lúc nghĩ = "a very bad idea" nguyên văn.
- Card F nc_card_degree_two (sau b20): degree-two = S nhả tùy thích + X giữ tới commit → chặn cascading nhưng mất serializability (T32 đọc Q hai lần hai giá — Fig 18.21) = read committed; quote đắt: index scan dưới degree-two có thể thấy HAI bản một dòng hoặc KHÔNG BẢN NÀO; cursor stability = S trên dòng cursor đang xử + X giữ tới commit — hàng nóng truy cập dày; câu chốt sách: snapshot isolation là lựa chọn tốt hơn cả hai. CTA tạm → /courses/db_design_nc — ĐỢT SAU (M9 mở) REPOINT về ?lesson=21.
- Khối bịa: nc_19 "hai giao dịch không đụng cùng dòng nào thì khỏi soát gì nhau" (write skew bác bỏ); nc_20 "giữ khóa ghế suốt lúc nghĩ cho chắc — 5 phút có là bao" (nguyên văn very bad idea).
- Trophy/ship: COURSE_MILESTONES trophies {10:1, 20:2} — verify overlay trophy khi submit step-4 nc_20.

---
## ADDENDUM ĐỢT 12 (2026-07-07) — MỞ MODULE 9 "Hộp Đen": nc_21 Failure & Storage + nc_22 Log/Undo/Redo + Card G
User chốt 4/4 recommended: (1) nc_21+nc_22+Card G mở M9, module_title "Hộp Đen — Sự cố & Phục hồi" (module: 9, màu Emerald #34D399 đã khai), trả nợ repoint Card F → ?lesson=21; (2) sim thứ 12 `storage_visual` — 3 tầng RAM/ĐĨA/STABLE + 2 mode CRASH SỚM/MUỘN; (3) sim thứ 13 `recovery_visual` — máy quét log Fig 19.4, renderer TỰ quyết undo/redo; (4) step-4: nc_21 mcq_code (crash-at-X survival) + nc_22 fill_blank (bản án undo/redo Fig 19.4). Rotation: nc_20 fill_blank → nc_21 mcq_code → nc_22 fill_blank ✓. KHÔNG probe engine (recovery = khái niệm, step-4 mcq/fill không chạy PE_runSQL).

Ràng buộc: M9 bài 21-24 KHÔNG có trophy giữa (COURSE_MILESTONES.db_design_nc trophies {10:1,20:2}); graduation 25 = boss. Nên nc_21-24 = lesson thường, không nổ overlay. shipLabel(9) = MARKETPLACE v3.0 nhưng chỉ nổ ở graduation 25.

Số canonical M9 (bám sách Ch.19.2-19.3, skin GameHub — server sập đêm flash-sale 23:47):
- nc_21 (Ticket #62) — storage 3 tầng: VOLATILE (RAM buffer — mất sạch khi crash/mất điện) · NON-VOLATILE (đĩa — sống qua crash phần mềm nhờ fail-stop, chết nếu đĩa hỏng/head crash) · STABLE (2 bản sao mirror + remote — sống cả hỏa hoạn/lụt). Ví "Quỹ Sàn" (A) = 1000 gem trên đĩa; giao dịch trừ 50 → 950. read(A) nạp 1000 lên buffer → tính 950 trong buffer (đĩa vẫn 1000) → write(A) 950 vào buffer block (CHƯA output). Mode CRASH SỚM (sau write, trước output): RAM bay → khởi động đọc đĩa = 1000, giá trị 950 BỐC HƠI. Mode CRASH MUỘN (sau output(A)): đĩa = 950 → sống. fail-stop = crash làm HALT nhưng không hỏng non-volatile. Điểm chốt (nối nc_22): không thể biết trạng thái từ database — cần LOG.
- nc_21 step-4 mcq_code: "server sập đúng sau write(ví=950) vào buffer, TRƯỚC output — khởi động lại ví hiện bao nhiêu?" đúng = 1000 (950 chỉ trong RAM buffer chưa output, bay theo crash; đĩa còn giá cũ). Bẫy: 950 ("write đã ghi nên xuống đĩa" — sai, write chỉ vào buffer); 0 ("crash xóa sạch" — sai, non-volatile intact); "không xác định".
- nc_22 (Ticket #63) — log records + undo/redo (Fig 19.2-19.4). Ví A=1000, B=2000, C=700 (nối nc_21 A=Quỹ Sàn). T0 chuyển 50 A→B: log <T0 start> <T0,A,1000,950> <T0,B,2000,2050> <T0 commit>. T1 rút 100 từ C: <T1 start> <T1,C,700,600> <T1 commit>. Update log record = <Ti, Xj, V1(cũ), V2(mới)>. Log ghi TRƯỚC data (seed WAL, đầy đủ để dành bài 23). Commit = <Ti commit> ra stable storage = điểm commit atomic. redo = đặt giá trị MỚI; undo = khôi phục giá trị CŨ. Luật phục hồi 3 ca (renderer TỰ quyết): UNDO nếu có <start> mà KHÔNG có commit/abort; REDO nếu có <start> VÀ (commit HOẶC abort). Fig 19.4: (a) crash sau <T0,B,..> → undo(T0): A1000 B2000 C700; (b) crash sau <T1,C,..> → redo(T0)+undo(T1): A950 B2050 C700; (c) crash sau <T1 commit> → redo(T0)+redo(T1): A950 B2050 C600.
- nc_22 step-4 fill_blank (ca b — có cả undo lẫn redo): T0 hành động = REDO; T1 hành động = UNDO; C chốt = 700.
- Card G nc_card_shadow_paging (sau b22, Note 19.1): shadow copy (text editor: save=commit, quit-no-save=abort) → chép CẢ database, đắt; shadow paging (page table, chỉ chép page bị sửa + swap db-pointer atomic) → không hợp concurrent transaction, DB không dùng; DB thật dùng log-based. CTA tạm → /courses/db_design_nc — ĐỢT SAU repoint ?lesson=23.
- Khối bịa: nc_21 "write(X) xong là dữ liệu chắc chắn nằm trên đĩa rồi" (sai — write chỉ vào buffer, output mới xuống đĩa); nc_22 "cứ sửa thẳng database, hỏng thì lát tính" (sai — phải ghi log TRƯỚC khi sửa, mới undo/redo được).
- module_title M9 = "Hộp Đen — Sự cố & Phục hồi". story.tag = "🎫 GameHub Marketplace · Ticket #62/#63". Card map M9 (PART_7): G Shadow Paging (b22) · H Force/No-force+Steal/No-steal (b23) · I Database Dump+Remote Backup (b23) · J Physical vs Logical Undo (b24) · K Main-Memory Recovery (cuối) · L Online Index Creation (cuối).

---
## ADDENDUM ĐỢT 13 (2026-07-07) — nc_23 WAL/Checkpoint + nc_24 ARIES + Card H/I/J — KHÉP DẠY M9
User chốt 4/4 recommended: (1) nc_23+nc_24+Card H/I/J khép phần dạy Recovery (sau đợt này M9 chỉ còn boss nc_25 = graduation v3.0), trả nợ repoint Card G → ?lesson=23; (2) sim thứ 14 `wal_visual` — CỔNG WAL (log phải xuống trước data), 2 mode THEO WAL/PHÁ WAL; (3) sim thứ 15 `aries_visual` — bảng điều khiển 3-pass analysis→redo→undo, khâu SKIP redo nhờ PageLSN; (4) step-4: nc_23 mcq_code (steal + WAL cứu/không) + nc_24 fill_blank (RedoLSN / số record skip / undo-list). Rotation: nc_22 fill → nc_23 mcq → nc_24 fill ✓. KHÔNG probe engine.

Số canonical M9 đợt 13 (bám sách Ch.19.5 + 19.9, ví A=Quỹ Sàn 1000, B=DragonForge 2000, C=NightRaven 700 nối nc_22):
- nc_23 (Ticket #64) — WAL rule (3 điều 19.5.1): (a) <Ti commit> ra stable thì txn mới commit; (b) trước <Ti commit>, MỌI log record của Ti phải ra stable; (c) TRƯỚC khi block data xuống đĩa, mọi log record liên quan block đó phải ra stable. Điều (c) = nền STEAL policy. force/no-force + steal/no-steal (19.5.2): chuẩn công nghiệp = NO-FORCE (block committed chưa cần xuống đĩa ngay → cần REDO) + STEAL (block chưa-commit được phép xuống đĩa → cần UNDO); cả hai đòi WAL. Checkpoint (19.3.6): <checkpoint L> → recovery chỉ quét từ checkpoint gần nhất; txn xong trước checkpoint khỏi redo. Crash recovery 2 pha (19.4.2): REDO xuôi từ checkpoint (dựng undo-list: gặp start→thêm, commit/abort→bớt) + UNDO ngược (roll back undo-list). Ví WAL đắt (sách printed 927-928): T0 sửa A 1000→950, buffer đầy → block A bị đẩy xuống đĩa (steal). THEO WAL: log <T0,A,1000,950> ra stable TRƯỚC → crash → recovery undo A về 1000 ✓. PHÁ WAL: block A=950 xuống đĩa TRƯỚC khi log record ra stable → crash → log mất record → không biết giá cũ → A kẹt 950, database 950/2000/700 SAI, không cứu.
- nc_23 sim wal_visual: 2 tầng LOG(stable)/ĐĨA + cổng; mode THEO WAL (steps flush log trước data, crash→undo được), mode PHÁ WAL (data trước log, crash→recovery thiếu record→950/2000/700 sai). Step-4 mcq_code: "block A=950 (T0 chưa commit) đã xuống đĩa nhưng log record CHƯA ra stable rồi crash — ví A?" đúng = kẹt 950 SAI (thiếu log→không undo được→đúng thảm họa WAL sinh ra để chặn). Bẫy: 1000 "chắc chắn undo được"; 950 "đúng vì đã ghi"; 0.
- nc_24 (Ticket #65) — ARIES (19.9). 4 khác biệt: LSN (định danh log record, tăng dần) + PageLSN (mỗi trang nhớ LSN cú sửa cuối) → redo BỎ QUA record có LSN ≤ PageLSN trang (đã phản ánh trên đĩa); physiological redo; DirtyPageTable (trang bẩn + RecLSN) giảm redo thừa; fuzzy checkpoint (chỉ ghi DPT, không flush trang). 3 pass: ANALYSIS (tìm checkpoint → RedoLSN = min RecLSN trong DPT → undo-list) · REDO (xuôi từ RedoLSN, mỗi record so LSN với PageLSN: ≤ skip, > redo) · UNDO (ngược, roll back undo-list, ghi CLR/UndoNextLSN). CLR = redo-only record lúc rollback.
- nc_24 canonical (rút gọn Fig 19.9-19.10): pages A(page4894)/B(page7200)/C(page2390); log LSN: 10 <T1 start> · 20 <T1,A,1000,900> · 30 <T2,B,2000,2050> · 40 <T2 commit> · 50 checkpoint{DPT: A RecLSN20, B RecLSN30; active T1} · 60 <T1,A,900,850> · 70 <T3 start> · 80 <T3,C,700,600> · 90 <T3 commit> · CRASH. Đĩa lúc crash: A PageLSN=20 (=900), B PageLSN=30 (=2050), C PageLSN=0 (=700). ANALYSIS: RedoLSN=min(20,30)=20; undo-list={T1} (T3 thêm rồi bớt vì commit); DPT thêm C(RecLSN80). REDO từ 20: LSN20 skip (≤ PageLSN A=20), LSN30 skip (≤ PageLSN B=30), LSN60 redo A=850 (60>20), LSN80 redo C=600. UNDO {T1}: LSN60 undo A 850→900, LSN20 undo A 900→1000, gặp <T1 start> ghi <T1 abort>. Final: A=1000 (T1 undone), B=2050, C=600. ARIES bỏ 2 cú redo thừa (LSN20,30) nhờ PageLSN.
- nc_24 step-4 fill_blank: RedoLSN = 20; số record redo bị SKIP = 2; undo-list cuối = T1.
- Card H nc_card_force_steal (sau b23): 2×2 force/steal; no-force→cần redo, steal→cần undo; chuẩn = no-force+steal, cả hai dựa WAL. Card I nc_card_dump_backup (sau b23, Ch.19.6-19.7): mất non-volatile → dump gần nhất + redo log từ dump; remote backup (primary/secondary, hot-spare, one-safe/two-safe) cho high availability. Card J nc_card_physical_logical_undo (sau b24, Ch.19.8): physical undo = ghi lại giá cũ từng byte; logical undo = undo bằng thao tác ngược (vd tăng→giảm) cho phép nhả khóa sớm; ARIES CLR. CTA H→b24, I→course(nợ repoint boss/graduation), J→course(nợ repoint boss). Card G đã repoint → ?lesson=23.
- Khối bịa: nc_23 "cứ đẩy block xuống đĩa lúc nào tiện, log theo sau cho nhanh" (phá WAL); nc_24 "redo lại TẤT CẢ record từ đầu log cho chắc ăn" (bỏ qua PageLSN skip + checkpoint → phục hồi chậm vô ích).
- module_title M9 = "Hộp Đen — Sự cố & Phục hồi". Ticket #64/#65.
