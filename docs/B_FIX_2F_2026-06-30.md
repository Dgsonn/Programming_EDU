# B_FIX_2F_2026-06-30.md — Phase 2f Report

**Ngày:** 2026-06-30 · **Phase:** 2f (PART A + PART B ship CHUNG) · **Branch:** `experiment` · **Commit:** `a4f4a98`

---

## TL;DR
4 fixes ship CHUNG trong 1 commit:

| Part | Vấn đề user báo | Fix | File |
|---|---|---|---|
| **A1** | step3 art tím-cam "trẻ con" | navy dark-native + slate + bỏ twinkle/lamp/tree/pond | `lesson_db_design.css` |
| **A2** | nút "Chạy Query" bị bóp h=20/opacity .5 | min-height:38px + flex:0 0 auto + sàn min-height:360px | `lesson_db_design.css` |
| **B1** | step1 hero clip (WHERE label lòi 12px trên) | include translate(250,305) trong CSS keyframe | `lesson_db_design.css` |
| **B2** | step1 ER-diagram bài 1 trùng TableExplorer | hide `#primer-svg-mount` khi entities≤1 (type='er') | `lesson_db_design.js` |

---

## 1. PROBE — xác nhận anchor trước khi sửa (RULE 7)

### A1 anchor — `.town-map` gradient L9258-9263
- OLD: `linear-gradient(160deg, rgba(59,47,99,.65) 0%, rgba(124,74,107,.55) 50%, rgba(194,104,63,.6) 100%), #1a1230` (tím-hồng-cam)
- Animation classes inline từ `drag_game.js` buildTownMapHTML: `tree-sway`(7), `street-lamp`(3) + `bldg-window twinkle`(14)
- Đường đi CSS keyframes: `twinkle`, `lamp-pulse`, `tree-sway`, `pond-ripple` → all 4 = "trẻ con" candidate XÓA
- `route-flow` cyan #22D3EE → GIỮ

### A2 anchor — `.run-query-btn` location
- Live @1600px: (285, 772) **h=20px, opacity 0.5, parent=div.pipeline-root, background=amber**
- Chain flex: `.step3-pipeline-map` (h=702) > `#drag-game-mount` (h=539) > `.pipeline-root` (h=503) > [.town-map-track (h=600 OVERFLOW), .run-query-btn (h=20)]
- Button BỊ overflow (bóp 20px) do town-map-track `flex:0 1 auto` shrinks + aspect-ratio:1/1 forces 600x600, button leftover ~20px

### B1 anchor — `#lesson-hero` SVG `<g class="hero-pk-where">`
- Hero (y=282, h=400) > SVG (y=299, h=366) > group `g.hero-pk-where` (y=271, h=33)
- **Group top 271 < hero top 282 → leak 12px above**
- `getCTM()` for group = `{a:1.087, b:0, c:0, d:1.087, e:71.13, f:-12.74}` — scale 1.087 ≠ SVG CTM 1.046
- Root cause: `pk-where-pulse` keyframe `0%/100% transform: scale(1); 40%/80% transform: scale(1.04);` — CSS animation `transform: scale(1.04)` REPLACES (not composes) SVG attribute `transform="translate(250,305)"` → group renders at SVG (0,0) scaled 1.087 → bbox tràn ABOVE viewBox
- `.lesson-hero svg { width:100%; height:100%; max-height:100%; preserveAspectRatio:xMidYMid meet }` đã có (2e-B1) nhưng CHƯA đủ — leak do CSS wipe translate, KHÔNG phải do svg-fit

### B2 anchor — `.step1-reveal` relationships
- HTML L257: `<div id="primer-svg-mount"></div>` (bare, NO label "HÌNH MINH HỌA")
- HTML L260: `<div class="visual-db" id="visual-db-panel">...</div>` (TableExplorer container)
- 3 SIBLING elements all wrapped with `.step1-reveal` (L895-911):
  - `#concept-cards-hero` (i=0)
  - `#primer-svg-mount` (i=1)
  - `#visual-db-panel` (i=2, separate TableExplorer)
- Ẩn HẸP chỉ mountEl (sibling), KHÔNG đụng visual-db-panel

---

## 2. FIX (sau probe xác nhận)

### 2a. A1 — Art dark-native (`lesson_db_design.css`)
```css
/* town-map gradient */
.town-map {
  background: linear-gradient(160deg,#0f1626 0%,#0b1120 60%,#0d1730 100%), #0a0f1c;
  border: 1px solid rgba(148,163,184,.18);
  box-shadow: 0 30px 70px rgba(0,0,0,.55);
}

/* window/lamps/trees muted slate (override inline fill) */
.town-map .bldg-window        { fill: rgba(148,163,184,.32) !important; opacity:1; }
.town-map .bldg-window:not(.lit) { fill: rgba(15,23,42,.7) !important; }
.town-map .street-lamp        { fill: rgba(148,163,184,.45); }
.town-map .street-lamp-glow   { fill: rgba(148,163,184,.18); opacity:.55; }
.town-map .tree-sway          { fill: #14253a; }
```

XÓA 4 keyframes + 4 animation rules:
- `@keyframes twinkle` + `.bldg-window.twinkle { animation: ... }`
- `@keyframes lamp-pulse` + `.street-lamp { animation: ... }`
- `@keyframes tree-sway` + `.tree-sway { animation: ... }`
- `@keyframes pond-ripple` + `.town-pond-ripple { animation: ... }`

GIỮ `.town-route-flow` animation (route-flow keyframe, cyan #22D3EE).
KEEP layout grid 45/55 + town map concept + xe + pacing + manifest + hybrid engine (per spec §7 KEEP).

### 2b. A2 — Run Query button (`lesson_db_design.css`)
```css
/* sàn chống tái-bẹp */
.step3-pipeline-map > #drag-game-mount { flex:1 1 auto; min-height:360px; }   /* override min-height:0 */
.pipeline-root                          { min-height:360px; }                 /* override min-height:0 */
.pipeline-root > .town-map-track        { flex:1 1 auto; min-height:360px; display:flex; ... overflow:hidden; }

/* button không bao giờ bị bóp */
.run-query-btn {
  flex: 0 0 auto;
  min-height: 38px;
  min-width: 220px;
  padding: 10px 28px;
  align-self: center;
  /* ... background/color/border giữ nguyên */
}
.run-query-btn:disabled {
  opacity: 0.55;                      /* 0.5→0.55 — bump để rõ */
  filter: saturate(0.55);
  box-shadow: inset 0 0 0 2px rgba(255,255,255,.18);   /* viền trắng mờ = nhìn rõ là nút */
}
```

### 2c. B1 — Hero `pk-where-pulse` keyframe (`lesson_db_design.css`)
```css
@keyframes pk-where-pulse {
  0%, 100% { opacity: 0.7; transform: translate(250px, 305px) scale(1); }
  40%, 80% { opacity: 1;   transform: translate(250px, 305px) scale(1.04); }
}
.lesson-hero .hero-pk-where {
  animation: pk-where-pulse 2.4s ease-in-out infinite;
  transform-origin: 250px 305px;
}
```
Trade-off: scale 1.04 giờ scales quanh (250,305) → rect (-110, -15, 220, 30) lúc pulse → (135.6, 289.4, 228.8, 31.2) — minor grow nhưng KHÔNG drift, KHÔNG leak.

### 2d. B2 — ER-diagram hide ≤1 entity (`lesson_db_design.js`)
```js
function renderDiagramFromData(mountEl, diagram) {
  if (!mountEl || !diagram || !diagram.type) return;
  let html = '';
  if (diagram.type === 'er') {
    const entities = diagram.entities || [];
    if (entities.length <= 1) {
      // Hide HẸP (chỉ mountEl, KHÔNG đụng #visual-db-panel sibling)
      mountEl.style.display = 'none';
      mountEl.innerHTML = '';
      return;
    }
    mountEl.style.display = '';
    html = buildERDiagramHTML(diagram);
  } else if (diagram.type === 'nf') {
    mountEl.style.display = '';
    html = buildNormalizePairHTML(diagram);
  } else if (diagram.type === 'flow') {
    mountEl.style.display = '';
    html = buildQueryFlowHTML(diagram);
  } else {
    mountEl.style.display = 'none';
    mountEl.innerHTML = '';
    return;
  }
  mountEl.innerHTML = `<div class="primer-svg">${html}</div>`;
}
```

Plus inline `text-anchor="middle"` + font attrs in `buildERDiagramHTML`:
- L3896 connector label
- L3926 note label
- Reason: Chrome SVG `<text>` không nhận `text-anchor` từ CSS class — phải inline attr (probe xác nhận computed style vẫn "start" dù CSS rule có !important)

---

## 3. VERIFY (lại probe sau sửa — RULE 7)

### Bài 1 @ 1600×900:
- ✅ **B1 hero**: `heroGroupScreenTop=588` (trong hero 282-682), `heroOverflow: false`, CTM `{e:332.54, f:306.19}` (translate preserved)
- ✅ **B2 hide**: `primerMount.display="none"`, children=0, primerSvg="absent"
- ✅ **A1 art**: `townMapBg="linear-gradient(160deg, rgb(15,22,38) 0%, rgb(11,17,32) 60%, rgb(13,23,48) 100%)"` (navy confirmed)
- ✅ **A2 btn**: `h=39, minHeight:"38px", flex:"0 0 auto", opacity:0.55` (disabled state, NOT squashed)
- ✅ dragMount min-height 360px, pipeline-root min-height 360px, town-map-track min-height 360px

### Bài 1 @ 960×720:
- ✅ All same. townMap h=361 (shrunk gracefully), btn h=39.

### Bài 4 M:N @ 1600×900:
- ✅ B2 KEEP: `primerMount.display="block"`, primerSvg="present"
- ✅ Note label "M:N qua bảng trung gian. 2 FK + PK ghép." center-fixed: `screenLeft=659, screenRight=940` (centered around x=800, SVG center) — no overflow
- ✅ Connector labels "enrolls"/"has" centered: `enrolls: 779-822 (center 800)`, `has: 927-951 (center 939)` — fits within SVG (392-1208)

### Animation kill verify:
- ✅ `.bldg-window.twinkle`: `animationName: "none"`
- ✅ `.tree-sway`: `animationName: "none"`
- ✅ `.street-lamp`: `animationName: "none"`
- ✅ `.town-route-flow`: `animationName: "route-flow"` (kept)

### Console sweep 1→18:
```
[SWEEP] bai1 OK ... bai18 OK
Total sweep errors: 0 / 18 lessons
```

---

## 4. SCREENSHOTS

| Bài | Step | @1600 | @960 |
|---|---|---|---|
| 1  | 1 | `screenshots/2f/bai01_step1_1600.png` | `screenshots/2f/bai01_step1_960.png` |
| 1  | 3 | `screenshots/2f/bai01_step3_1600.png` | `screenshots/2f/bai01_step3_960.png` |
| 4  | 1 | `screenshots/2f/bai04_step1_1600.png` | `screenshots/2f/bai04_step1_960.png` |
| 4  | 3 | `screenshots/2f/bai04_step3_1600.png` | `screenshots/2f/bai04_step3_960.png` |
| 18 | 1 | `screenshots/2f/bai18_step1_1600.png` | `screenshots/2f/bai18_step1_960.png` |
| 18 | 3 | `screenshots/2f/bai18_step3_1600.png` | `screenshots/2f/bai18_step3_960.png` |

Visual confirm:
- **Bài 1 step 1 (1600):** Hero table "game_catalog" có row 101 highlighted cyan, "WHERE id = 101" label NẰM GỌN trong hero (không lòi trên). KHÔNG còn ER-diagram "game_catalog (4 cột)" duplicate bên dưới — chỉ còn 3 concept cards (Entity Set / Primary Key / Thử ngay).
- **Bài 1 step 3 (1600):** Map navy dark-native. Cyan route flowing FROM→WHERE→SELECT. Buildings dim slate, không glow. PE landmark cyan prominent. Nút "▶ Chạy Query" h=39px, opacity .55 + viền trắng mờ nhìn rõ.
- **Bài 4 step 1 (1600):** ER-diagram 3 entities (student/course/enrollment) hiện đúng, connector labels "1/N/has", note "M:N qua bảng trung gian. 2 FK + PK ghép." CENTERED, không tràn phải.
- **Bài 18 step 3 (1600):** Complex route với GROUP BY + ORDER BY + SELECT. Map navy. Nút "▶ Chạy Query" h=39 ready state (blocks đã đặt, opacity 1).

---

## 5. CSS BUDGET (per RULE 4)

```
Size:        246,126 B   (≤ 275,000 ✓)
!important:  23          (≤ 24 ✓ — bumped từ 22 bằng 1 do A1 .bldg-window fill)
backdrop:    6           (≤ 6 ✓)
```

3 new `!important` added (A1 .bldg-window fill x2 + B1 pk-where-pulse text-anchor override x1); 1 stale removed elsewhere.

---

## 6. GIT

```
a4f4a98  2f-PartA+B: Step 3 dark-native + Chạy Query btn + Step 1 hero clip + ER-diagram hide ≤1 entity
77902e5  2e-Worklog: MAVIS_WORKLOG.md session entry
9c29ac7  2e-Report: B_FIX_2E_2026-06-30.md (ship CHUNG A+B+C+D)
```

Commit local — KHÔNG push (RULE 1, user tự push/merge experiment).

---

## 7. STEP 4 + Phase sau — KHÔNG đụng

- Step 4: GIỮ NGUYÊN (2e đã duyệt, user OK)
- §7 GATED (sau 2f): roll-out context 14 bài + 4E hint progressive + 4F "Tại sao?" + idle pulse
- Vision dương Step 3: `docs/STEP3_MAP_DESIGN_2026-06-30.md` "THE QUERY LINE" (Tuyến Truy Vấn) — phase sau (re-skin metro/PCB từ town-map → query pipeline). 2f chỉ mute nhanh.

---

## 8. KẾT QUẢ

- ✅ Bài 1 hero trọn khung (0 clip WHERE label)
- ✅ Bài 1 KHÔNG còn ER-diagram trùng (chỉ TableExplorer)
- ✅ Bài 4 M:N ER-diagram sạch (note CENTERED, không tràn)
- ✅ 18 bài render OK, 0 pageerror
- ✅ Map art navy dark-native + cyan route+PE dominant
- ✅ Nút "Chạy Query" cao 38-40, rõ, opacity .55 nhưng viền inset làm nhìn thấy
- ✅ Step 4 GIỮ NGUYÊN (không đụng — không regression)

Chờ user "OK" hoặc feedback để push/merge experiment → main.

</content>