# B_FIX_2E_2026-06-30.md — PHASE 2e Report (SỬA REGRESSION 2d)

## Scope (per user chốt 2026-06-30, "ship CHUNG")
Full phase 2e = **Part A** (Step 3 layout vỡ) + **Part B** (Step 1 hero + renderSchemaTable) + **Part C** (Step 4 bỏ schema + context giàu) + **Part D** (renderMyCourses null + CSS cleanup).

Nguyên nhân gốc 2e-A1: thiếu `/*` opener ở `lesson_db_design.css` L8595 (Phase 2d edit bug) → CSS parser nuốt rule `.split-pane.step3-two-col { display: grid; 45fr 55fr }` → fallback về `.step3-two-col { display: flex }` v6.1 → cột 1 bị bóp 221px, data-preview đè map, IDE tràn mép 145px.

Thứ tự A→B→C→D vì:
- A là fix layout/root cause — nếu không fix, các step khác cũng vỡ visual
- B là defensive fix cho step 1 (visual ổn nhưng spec note dễ vỡ khi SVG nội dung lớn hơn mount)
- C là spec mới (bỏ schema panel + context data-driven) — phải sau layout ổn định
- D là dọn code, renderMyCourses pageerror (nổ mỗi lần vào lesson)

---

## Part A — Step 3 Layout Fix (commit e1ee0da)

### A1: Hợp nhất layout về MỘT rule grid
- File: `static/css/lesson_db_design.css` L8595
- Fix: thêm `/*` opener ngay trước `* FIX 4 v6.2 — BRILLIANT-STYLE ...`
- Verify: probe post-fix cho thấy `.split-pane.step3-two-col` rule (0,2,0) thắng, `display: grid; grid-template-columns: 45fr 55fr` applied
- Backup: code cũ giữ comment trong `__backup/v6_1_2026-06-29/`

### A2: data-preview dưới map cột trái (per Q1=a)
- File: `templates/lesson_db_design.html` L361-377
- HTML: move `#step3-data-preview` từ `step3-topbar` → `step3-pipeline-map` (sau `drag-game-mount`)
- File: `static/css/lesson_db_design.css` L8629-8683
- CSS pipeline-map: thêm `min-width:0; min-height:0; display:flex; flex-direction:column; gap:10px`
- CSS drag-mount: `flex:1 1 auto` để map fill available
- CSS data-preview in pipeline: `flex:0 0 auto; max-width:100%` + ẩn label + compact font 11.5px

### A3: IDE defensive overflow
- File: `static/css/lesson_db_design.css` L8662-8671
- `#ide-code`, `.ide-display`, `.ide-content`: `min-width:0; max-width:100%; overflow-x:auto; overflow-wrap:anywhere; word-break:break-word`
- `.ide-pane.step3-ide`: thêm `min-width:0`

### A-ACCEPTANCE @1600px:
| Element | Pre-2e | Post-2e | Spec |
|---|---|---|---|
| `.town-map` (w×h) | 152×152 | **600×600** | ≥600×600 ✓ |
| `#ide-code` right | 1745 | **1584** | ≤1600 ✓ |
| map↔data-preview overlap | 2730px² | **0** | none ✓ |

Screenshot: `screenshots/probe_2e_postA_step3.png` (map dusk đầy đủ + data-preview compact dưới)

---

## Part B — Step 1 Hero + renderSchemaTable (commit f446140)

### B1: HERO clip tràn viền — defensive CSS
- File: `static/css/lesson_db_design.css` L7308-7314
- Đổi `.lesson-hero svg { max-height: 320px }` → `{ max-height: 100% }` (để SVG fill mount)
- Thêm `preserveAspectRatio: xMidYMid meet` explicit (không rely on user-agent default)
- Mount `.lesson-hero { overflow: hidden }` đã có → giữ

### B2: renderSchemaTable — defensive CSS
- File: `static/css/lesson_db_design.css` L724-758
- `.schema-table`: thêm `display:flex; flex-direction:column; gap; min-height:0; KHÔNG overflow:hidden cứng`
- `#schema-table-name`: thêm `display:inline-block; position:static` (span inline → flex item, không absolute đè rows)
- `.schema-row`: `position:static; margin:0` (parent gap xử lý spacing)
- Verify fallback mode (block table_explorer.js): 4 rows stack cleanly dưới panel-head, KHÔNG overlap, data-panel song song OK
- Note: visual-db-panel primary path là TableExplorer (override), nên schema-table defensive fix chỉ áp dụng khi TableExplorer fail/block

### B-ACCEPTANCE @1600px:
- Hero nằm trọn card (no clip) ✓
- 4 rows + cyan strip accent + 🔑 PK highlight all visible ✓
- Fallback mode (TableExplorer blocked): 4 schema rows stack cleanly, schema-table-name separate row ✓

---

## Part C — Step 4 Bỏ Schema + Context Giàu (commit 3090697)

### C1: Bỏ hẳn panel schema (DORMANT giữ hàm)
- File: `templates/lesson_db_design.html` L511-528
- HTML: xóa `.schema-pane` + `#step4-schema` khỏi cột 3, cột 3 = chỉ `#step4-results` full
- File: `static/css/lesson_db_design.css` L3048-3049
- CSS: thêm `.results-pane.results-pane--full { flex: 1 1 100% }`
- File: `static/js/lesson_db_design.js` L2671-2690
- JS: gỡ call site `renderStep4Schema` + `enhanceStep4Schema`; thêm comment DORMANT 2e-C1
- File: `static/js/lesson_db_design.js` L2706, L4206
- JS: đánh dấu function declarations DORMANT với comment giải thích
- KHÔNG xóa `.schema-pane` rules (dormant cùng hàm, dư budget 27KB, phase cleanup sau)

### C2: Context giàu data-driven — 4 bài (Bài 1/3/4/10)
- File: `static/js/lesson_db_design.js` L2704-2750
- Function mới: `renderStep4Context(s4)` → render vào `#step4-instructions`
- Schema: `s4.context = { scenario, steps[], hint_explore, example{question,sql,sample_output}, expected }`
- File: `static/js/lesson_content.js` Bài 1 (L186), Bài 3 (L816), Bài 4 (L1041), Bài 10 (L2135)
- 4 tag màu: BỐI CẢNH (cyan) / CÁC BƯỚC (indigo) / VÍ DỤ TƯƠNG TỰ (amber) / KẾT QUẢ MONG ĐỐI (emerald)
- `hint_explore` CHỈ hiện cho `full_ide` (mcq/fill_blank/bug_fix skip)
- 14 bài thiếu context → fallback `#step4-instructions` = rỗng, KHÔNG crash
- Lý do chọn 4 bài: Bài 1 (template chuẩn vàng) + Bài 3 (mid JOIN/FK) + Bài 4 (M:N composite key) + Bài 10 (late BCNF aggregation). Phủ 4 concept khác nhau để stress-test template.
- Lưu ý: data hiện tại 18/18 bài đều `full_ide` (không có non-full_ide ngoài đời) — render code support hint_explore skip cho non-full_ide khi tương lai có bài mcq/fill_blank/bug_fix

### C3: Sửa "bên phải" → "cột giữa"
- File: `static/js/lesson_content.js` L186, L817, L1007, L1228, L1432 (5 chỗ)
- Replace `trong editor bên phải.` → `trong editor ở cột giữa.`

### C-ACCEPTANCE:
- Cột 3 KHÔNG còn schema panel (chỉ Results) ✓
- Bài 1 hiển thị 5 context blocks (scenario + steps + hint_explore + example + output) ✓
- Smoke test gõ `SELECT * FROM game_catalog` → Run → 4 dòng × 4 cột (id/name/genre/price) ✓
- Smoke test gõ `SELECT name, price FROM game_catalog WHERE id = 101` → Run → 1 dòng × 2 cột (Elden Ring, 60) ✓
- 0 copy "bên phải" trong source ✓

Screenshots:
- `screenshots/probe_2e_postC_step4.png` (idle state, 5 context blocks)
- `screenshots/probe_2e_postD_step4.png` (same, after Part D)

---

## Part D — Cleanup (commit 076aa50)

### D1: renderMyCourses null guard
- File: `static/js/main.js` L1125-1133
- Trước: `container.innerHTML = ...` không guard → null crash mỗi lần vào lesson
- Sau: `if (!container) return;` trước khi `.innerHTML`
- Verify: 0 pageerror (pre-fix: 1× "Cannot set properties of null (setting 'innerHTML')")

### D2: CSS cleanup — remove duplicate `.step3-two-col` flex rule
- File: `static/css/lesson_db_design.css` L8570-8594 (v6.1 layout 2 cột)
- Comment thành DORMANT/REMOVED 2e-A1 (chỉ còn `.split-pane.step3-two-col` grid 45fr 55fr)
- Verify: probe postD cho thấy step3 layout 600×600 map, no regression

---

## Final State — Ship CHUNG (A+B+C+D)

### Files Modified
| File | Size | Notes |
|---|---|---|
| `static/css/lesson_db_design.css` | 254,757 B (budget 275,000 B ✓ headroom 20,243) | !important=20 ✓, backdrop-filter=6 ✓; A1 (/* + grid 45/55) + A2 (pipeline-map flex + data-preview) + A3 (IDE min-width:0) + B1 (svg max-height 100% + preserveAspectRatio) + B2 (.schema-table flex col) + C1 (.results-pane--full 100%) + C2 (context blocks 4 màu) + D2 (DORMANT comment for v6.1 flex) |
| `static/js/lesson_db_design.js` | ~226 KB (with renderStep4Context + DORMANT comments) | A2-A3 (IDE min-width via CSS, no JS change), C1 (gỡ call site + DORMANT comment), C2 (renderStep4Context) |
| `static/js/lesson_content.js` | (text replace) | C2 (4 context blocks) + C3 (5× "bên phải" → "cột giữa") |
| `static/js/main.js` | +1 guard | D1 (renderMyCourses null guard) |
| `templates/lesson_db_design.html` | (DOM change) | A2 (move data-preview to pipeline-map) + C1 (xóa .schema-pane + #step4-schema) |

### Commits (LOCAL ONLY, no push per RULE 1)
```
076aa50 2e-PartD: guard renderMyCourses null + remove duplicate .step3-two-col flex rule
3090697 2e-PartC: step4 bỏ schema panel + context giàu data-driven Bài 1/3/4/10
f446140 2e-PartB: step1 hero fit mount + renderSchemaTable defensive fix
e1ee0da 2e-PartA: step3 layout fix — comment /* + grid 45/55 + data-preview dưới map
```

### Backward Compatibility
- 18 bài × 4 steps = 72 lesson views
- 14 bài thiếu `s4.context` → fallback rỗng, KHÔNG crash (data-driven pattern)
- Step 4 schema engine (`PE_runSQL` + `enhanceStep4Schema` DORMANT) vẫn đọc `s4.schema.data` đúng
- Bài 7 step 4 cross-check vẫn pass (PE_runSQL WHERE parser đa schema)

### Console state
- Pre-2e: 9 issues (1 pageerror + 8 warnings)
- Post-2e: 4 issues (0 pageerror + 4 pre-existing warnings: 2 Chatbot Gemini key + 2 AudioContext autoplay)
- 0 pageerror mới từ 2e ✓

### Screenshots @1600px (cho mỗi part acceptance)
- `screenshots/probe_2e_pre_*.png` — baseline (3 files)
- `screenshots/probe_2e_postA_*.png` — Part A ship (3 files)
- `screenshots/probe_2e_postC_*.png` — Part C ship (3 files)
- `screenshots/probe_2e_postD_*.png` — final (3 files) + `probe_step4_smoke.png` (smoke test 2 query)

### Known limitations (ngoài scope 2e)
- `mcq_code`/`fill_blank`/`bug_fix` không có IDE editor (per 2d) → Results idle khi click option (mcq) hoặc giữ starter (fill/bug) — không sửa ở 2e
- `prompt` 14 bài còn lại CHƯA có context giàu — fallback rỗng OK, user duyệt template rồi mới roll out
- WHERE label trong SVG bài 1 thỉnh thoảng pulse ở opacity 0 (CSS animation timing) — không clip, chỉ missing 1 frame
- Town map có thể clip 1-2px đáy (drag-mount h=539 vs town-map h=600, drag-mount overflow:hidden) — visually OK vì data-preview compact vẫn hiện dưới

### Mid-ship discoveries
- Root cause A1 là CSS comment-delimiter bug đã từng gặp (memory note 2026-06-28) — đã áp dụng pattern: scan orphan ` *` line + missing `/*` opener
- L8570 + L8595 cùng chứa `* FIX 4 v6.x — ...` block comment; L8570 có `/*` opener (close L8568), L8595 KHÔNG có `/*` opener → parser nuốt
- Backup ở `__backup/v6_1_2026-06-29/` cho rollback nếu cần

---

## §7 (GATED — sau 2e OK)
4E hint progressive · 4F panel "Tại sao?" · idle pulse trạm/xe · path route bài 5-station.

---

*v47 PHASE 2e — **A)** step3: comment /* + grid 45/55 + data-preview dưới map (root cause: missing /* opener L8595) · **B)** step1: SVG fit + renderSchemaTable defensive · **C)** step4: bỏ schema + context data-driven 4 bài + sửa "bên phải"→"giữa" · **D)** renderMyCourses null + dọn CSS trùng. Ship CHUNG 4 commit, no push, 0 pageerror.*
