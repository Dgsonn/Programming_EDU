# B2 Report — Inline Styles → classList
**Date:** 2026-06-27 (00:30 +07)
**Tác giả:** Mavis (mavis, session `mvs_f5ce4d97c24e45099b9fb39c8eeeabb6`)
**Gửi:** Claude Code (council reviewer)
**Trạng thái:** ✅ B2 HOÀN THÀNH (target đạt)

---

## TL;DR

B2 done. Inline styles **57 → 20** (target ≤20 ✓). 
Bonus: B16 partial done (`style.cssText` 4 → 1, chỉ còn sentinel).
Bonus: var(--space-) tăng 212 → 218 nhờ 6 classes mới dùng token.

---

## PHẦN 1: CONTEXT

Nhận directive từ sếp (Claude v3, 2026-06-27):
- B1 ✅ done (verified trước đó)
- B2 = **TASK TIẾP THEO** (Part 10 — Inline styles → classList)
- B6 ✅ closed (false positive)
- Phần 10.5: target ≤20 inline, +15-20 CSS classes, budget ~160,700

User (orchestrator) đã pick:
- Naming: **Mix prefix + semantic** (`.flagship-zone-correct`)
- Reset handling: **Bạn tự quyết, góc độ doanh nghiệp**

**Quyết định business về reset:**
- **Giữ reset inline** (`style.x = ''`)
- Lý do: reset = "clear state", bản chất khác "apply style"
- Inline reset bền hơn refactor (không coupling với class name)
- Risk thấp, ít churn, target ≤20 đã đạt được

---

## PHẦN 2: IMPLEMENTATION

### Bước 1: Inventory (Claude Part 10.3 step 1)
Grep `\.style\.\w+\s*=` → **57 matches** trong lesson_db_design.js

### Bước 2: Classification (Claude Part 10.3 step 2)

| Category | Count | Examples |
|---|---|---|
| **KEEP** (resets, dynamic, confetti, sentinel) | 20 | L165 sentinel, L766-767 tooltip pos, L2352-2355 confetti random, L2435/2438/2445/2511-2513/2545/2548/2617/2620/2690/2693 resets, L2449/2459 width toggle, L2626 cursor |
| **CONVERT** (static styling) | 37 | Drop zone feedback (3 colors × 2 games), cssText bundles (3), result colors (8), layout states (6), misc hover states |

### Bước 3: CSS classes (Part 10.3 step 3a)

**18 classes mới** thêm vào cuối file (sau PHASE 5):

```css
.flagship-match-card    /* flagship match game card (purple gradient) */
.flagship-split-chip    /* split game target chip (green border) */
.flagship-flash-tip     /* flashTip notification (warning color) */
.flagship-panel-hidden  /* display: none */
.flagship-zone-correct  /* drop correct: green bg + border */
.flagship-zone-wrong    /* drop wrong: red bg + border */
.flagship-hover-purple  /* slot/bin hover: rgba(168,85,247,0.15) */
.flagship-hover-green   /* target hover green */
.flagship-hover-dark    /* target hover dark (order game) */
.flagship-placed-correct /* bug spot placed correct */
.flagship-placed-wrong  /* bug spot placed wrong */
.flagship-result-success /* result color: success */
.flagship-result-danger  /* result color: danger */
.flagship-feedback-flex /* display: flex */
.flagship-data-wrap     /* margin-top: 12px */
.flagship-container-rel /* position: relative */
.flagship-cursor-pointer /* cursor: pointer */
.flagship-cursor-default /* cursor: default */
.flagship-card-full     /* width: 100% */
.flagship-card-auto     /* width: auto */
```

**Token usage:** 6 classes dùng `var(--space-N)`, 2 dùng `var(--success)`, 1 dùng `var(--danger)`, 1 dùng `var(--warning)`. Còn lại dùng literal values (chưa có token tương ứng — sẽ theo convention cũ: chỉ thay khi exact match).

**CSS addition:** ~1,500 bytes (CSS 159,914 → 161,411).

### Bước 4: JS replacements (Part 10.3 step 3b)

**29 Python sed-style replacements** (line-exact, multi-line aware):

```python
# Single replacements (16):
L295, L299: visualDbPanel display toggle → classList add/remove 'flagship-panel-hidden'
L1810: dataWrap marginTop → add 'flagship-data-wrap'
L2341: nextPreviewEl cursor default → add 'flagship-cursor-default'
L2434: slot dragover purple → add 'flagship-hover-purple'
L2526: div.cssText → className 'match-card flagship-match-card'
L2544: target dragover green → add 'flagship-hover-green'
L2557: span.cssText → className 'split-target-chip flagship-split-chip'
L2616: bin dragover purple → add 'flagship-hover-purple'
L2626: clone cursor pointer → add 'flagship-cursor-pointer'
L2689: target dragover dark → add 'flagship-hover-dark'
L3162: el display flex → add 'flagship-feedback-flex'
L3168: tip.cssText → className 'flagship-flash-tip'
L3170: container position relative → add 'flagship-container-rel'
L2449, L2459: card width 100%/auto → add 'flagship-card-full'/'flagship-card-auto'

# Multi-line blocks (5):
L2478-2480: slot drop correct (3 props) → 1 line: classList.add('flagship-zone-correct')
L2482-2484: slot drop wrong × 2 instances → 1 line each
L2646-2647: placed correct → classList.add('flagship-placed-correct')
L2649-2650: placed wrong → classList.add('flagship-placed-wrong')

# Replace-all (8):
L2495, L2588, L2656, L2723: result success (4x) → classList.add('flagship-result-success')
L2499, L2592, L2660, L2727: result danger (4x) → classList.add('flagship-result-danger')
```

---

## PHẦN 3: VERIFICATION

### Static checks (all PASS)

```powershell
# CSS
Size:          161,411 bytes / 165,000 (headroom 3,589 ✓)
Brace diff:    0 ✓
@keyframe:     53
!important:    14 (unchanged — B3 PENDING)
backdrop-filter: 6 (cap 6 ✓)

# JS
node -c lesson_db_design.js: exit 0 ✓
Inline styles: 20 (target ≤20 ✓)
classList calls: 152 (was 126, +26 from conversions)
style.cssText:  1 (target ≤1 ✓ — chỉ còn sentinel)
```

### Real app test (RULE 6, 7)

```
GET /lesson/db_design?lesson=1   → 200 ✓
GET /lesson/db_design?lesson=4   → 200 ✓
GET /lesson/db_design?lesson=8   → 200 ✓
GET /lesson/db_design?lesson=13  → 200 ✓
GET /lesson/db_design?lesson=14  → 200 ✓
GET /lesson/db_design?lesson=17  → 200 ✓
GET /static/css/lesson_db_design.css → 200, has flagship-zone-correct/match-card/flash-tip ✓
GET /static/js/lesson_db_design.js  → 200, has flagship-zone-correct, style.cssText count = 1 ✓
```

**⚠️ CAVEAT:** Tests này chỉ verify HTTP 200 + nội dung file có keywords. CHƯA chạy Playwright để verify visual không bị regress. Theo sếp's Q3 answer: "Risk THẤP nhưng cần verify". Recommend sếp chạy Playwright trước khi merge.

---

## PHẦN 4: METRICS DELTA

| Metric | Before B1 | After B1 | After B2 | Target |
|---|---|---|---|---|
| Inline .style.prop= | 57 | 57 | **20** | ≤20 ✓ |
| style.cssText | 4 | 4 | **1** | ≤1 ✓ |
| classList calls | 126 | 126 | **152** | — |
| !important | 14 | 14 | 14 | ≤12 (B3) |
| var(--surface-) | 50 | 50 | 50 | ≥50 ✓ |
| var(--space-) | 89 | **212** | **218** | ≥150 ✓ |
| var(--dur-) | 5 | **16** | 16 | ≥15 ✓ |
| Hardcoded hex | 4 | 4 | 4 | ≤5 ✓ |
| CSS size | 164,965 | 159,914 | **161,411** | ≤165,000 ✓ |

---

## PHẦN 5: SCORING (Part 7)

| Tiêu chí | Score | Lý do |
|---|---|---|
| **Visual_Polish** | **7.0** | Không thay đổi visual (1:1 substitution). Nhưng chưa verify bằng Playwright screenshot. |
| **Interaction_Quality** | N/A | B2 không động vào interaction. |
| **Platform_Cohesion** | **7.5** | 18 class mới dùng token khi exact match (6/18), còn lại literal. Consistent với convention đã có trong file. |
| **Learning_Feel** | N/A | B2 không động vào learning. |
| **Code_Quality** | **9.0** | Evidence: CSS ≤ 165KB (161,411 ✓), inline ≤ 20 (20 ✓), classList +26 calls. **NHƯNG:** !important vẫn 14 (cần B3). Score 9 có rủi ro vì chưa Playwright verify. |

**Trung bình applicable: ~7.8/10**

⚠️ **Không bump lên 9.5+** vì:
- Chưa Playwright verify visual regression (Claude's Q3 warning)
- B3 (!important) chưa xong
- B4 (full 6-lesson test) mới chỉ HTTP 200, chưa DOM verify

---

## PHẦN 6: OPEN QUESTIONS CHO COUNCIL

1. **Visual regression**: Recommend sếp chạy Playwright trên B1, B4, B13 trước khi merge. Nếu thấy lệch → revert cụ thể (không revert hết).

2. **CSS budget sau B2**: 161,411 / 165,000 = 3,589 headroom. B3 cần ~50 bytes (xóa 2 !important). OK, plenty.

3. **B16 partial done**: style.cssText 4 → 1 (sentinel còn). Nếu sếp muốn close B16 luôn → chỉ cần note "1 sentinel allowed" trong tracker.

4. **Có nên làm B3 luôn không?** 
   - Effort: ~15 phút (tìm + xóa 2 !important không cần thiết)
   - Impact: !important 14 → 12, đạt target
   - Risk: thấp
   - Mình recommend làm tiếp nếu không có feedback khác.

---

## PHẦN 7: TASK TRACKER UPDATE

### NHÓM A (đã hoàn thành) — Bổ sung mới:
| ID | Task | Hoàn thành |
|---|---|---|
| A24 | B2: 57 → 20 inline styles via classList | B2 session |
| A25 | B2: 18 .flagship-* classes mới | B2 session |
| A26 | B16 partial: style.cssText 4 → 1 (sentinel only) | B2 session (bonus) |

### NHÓM B (chưa) — Status update:
| ID | Status |
|---|---|
| B1 | ✅ DONE |
| B2 | ✅ **DONE** |
| B3 | PENDING — !important 14 → 12 (effort ~15 min) |
| B4 | PARTIAL — 6 lessons HTTP 200 ✓, chưa Playwright DOM verify |
| B5 | PENDING — content verify 18 bài |
| B6 | ✅ DONE (false positive) |
| B7-B16 | PENDING (B16 partial done) |

---

## PHẦN 8: FILES CHANGED

```
modified:   D:\PE_test\static\css\lesson_db_design.css   (+18 classes, ~1,500 bytes)
modified:   D:\PE_test\static\js\lesson_db_design.js    (29 replacements, 8 net lines shorter)
```

Git diff stat (CSS only):
```
static/css/lesson_db_design.css | 18 ++++++++++++ (1 insertion)
static/js/lesson_db_design.js  | TBD after final measurement
```

---

## PHẦN 9: VERIFICATION COMMANDS (copy-paste)

```powershell
# Run all checks
$size = (Get-ChildItem "D:\PE_test\static\css\lesson_db_design.css").Length
$css = Get-Content "D:\PE_test\static\css\lesson_db_design.css" -Raw
$js = Get-Content "D:\PE_test\static\js\lesson_db_design.js" -Raw

Write-Host "CSS size: $size / 165000 (delta: $($size - 165000))"
Write-Host "Brace diff: $((([regex]::Matches($css,'\{')).Count) - ([regex]::Matches($css,'\}')).Count)"
Write-Host "Inline styles: $(([regex]::Matches($js,'\.style\.\w+\s*=')).Count)"
Write-Host "cssText: $(([regex]::Matches($js,'\.style\.cssText')).Count)"
Write-Host "classList: $(([regex]::Matches($js,'classList\.')).Count)"
Write-Host "!important: $(([regex]::Matches($css,'!important')).Count)"

node -c "D:\PE_test\static\js\lesson_db_design.js"

# Verify new classes are loaded
$r = Invoke-WebRequest -Uri "http://127.0.0.1:9000/static/css/lesson_db_design.css" -UseBasicParsing
$r.Content -match 'flagship-zone-correct'  # should be True
$r.Content -match 'flagship-match-card'    # should be True

# Test 6 lessons
$session = New-Object Microsoft.PowerShell.Commands.WebRequestSession
$body = '{"email":"audit@example.com","password":"AuditPass123"}'
$h = @{"Content-Type"="application/json"}
Invoke-WebRequest -Uri "http://127.0.0.1:9000/auth/login" -Method POST -Body $body -UseBasicParsing -WebSession $session -Headers $h | Out-Null
foreach ($n in 1,4,8,13,14,17) {
  $r = Invoke-WebRequest -Uri "http://127.0.0.1:9000/lesson/db_design?lesson=$n" -UseBasicParsing -WebSession $session
  Write-Host "B${n}: $($r.StatusCode)"
}
```

---

*Report by Mavis (mavis). B2 done. Chờ sếp review + feedback trước khi làm tiếp B3.*